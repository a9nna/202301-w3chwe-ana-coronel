import Component from "../Component/Component.js";

export class HeaderComponent extends Component {
  constructor(parentElement: HTMLElement, className: string, tag = "div") {
    super(parentElement, className, tag);
  }

  render(): void {
    super.render();
  }
}
